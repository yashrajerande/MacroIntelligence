{
  "type": "array",
  "minItems": 7,
  "maxItems": 7,
  "items": {
    "type": "object",
    "required": ["signal_num", "signal_theme", "status", "title", "data_text", "implication", "pct_10y", "pct_note", "is_surprise"],
    "properties": {
      "signal_num":   { "type": "integer", "minimum": 1, "maximum": 7 },
      "signal_theme": { "type": "string" },
      "status":       { "type": "string", "enum": ["positive", "risk", "watch", "surprise"] },
      "title":        { "type": "string" },
      "data_text":    { "type": "string" },
      "implication":  { "type": "string" },
      "pct_10y":      { "type": "integer", "minimum": 0, "maximum": 100 },
      "pct_note":     { "type": "string" },
      "is_surprise":  { "type": "boolean" }
    }
  }
}
